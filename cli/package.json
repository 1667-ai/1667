{
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun src/standalone.ts",
    "build:standalone": "bun scripts/build-standalone.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "bun": ">=1.4.0"
  }
}
